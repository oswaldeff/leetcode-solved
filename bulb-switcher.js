info = {
  descriptionUrl: "https://leetcode.com/problems/bulb-switcher/description/",
  createdAt: "2023-04-27",
  updatedAt: null,
};

/**
 * @param {number} n
 * @return {number}
 */

function bulbSwitch(n) {
  const bulbs = Array.from({ length: n }, () => 1);
  let i = 2;
  while (i <= n) {
    break;
  }
}
